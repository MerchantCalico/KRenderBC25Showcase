{
	"schemaVersion": 1,
	"id": "${mod_id}",
	"version": "${mod_version}+${minecraft_version}",
	"name": "${mod_name}",
	"description": "${mod_description}",
	"authors": [
		"${mod_author}"
	],
	"contributors": [
		"${mod_contributors}"
	],
	"contact": {
		"homepage": "https://github.com/${sources}",
		"issues": "https://github.com/${sources}/issues",
		"sources": "https://github.com/${sources}"
	},
	"license": "${mod_license}",
	"icon": "assets/${mod_id}/icon.png",
	"environment": "*",
	"entrypoints": {
		"main": [
			"net.merchantpug.krendershowcase.KRenderShowcase"
		],
		"client": [
			"net.merchantpug.krendershowcase.client.KRenderShowcaseClient"
		]
	},
	"depends": {
		"fabricloader": "${fabric_loader_range}",
		"fabric-api": "*",
		"minecraft": "${fabric_minecraft_version_range}",
		"java": ">=${java_version}"
	},
	"custom": {
		"modmenu": {
			"links": {
				"modmenu.github_releases": "https://github.com/${sources}/releases",
				"modmenu.kofi": "https://ko-fi.com/kneelawk"
			}
		}
	}
}